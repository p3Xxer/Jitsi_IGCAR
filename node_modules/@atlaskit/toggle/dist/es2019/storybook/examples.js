import React from 'react';
import { fireEvent } from '@testing-library/dom';
import Toggle from '../../src';
export default {
  title: 'Toggle'
};
export const toggle = () => {
  return /*#__PURE__*/React.createElement(Toggle, {
    testId: "my-toggle"
  });
};
const interactionTasks = [{
  name: 'Change toggle status',
  description: 'Switch a toggle on and off',
  run: async ({
    container
  }) => {
    const toggles = Array.from(container.querySelectorAll('[data-testid="my-toggle"]'));
    const last = toggles[toggles.length - 1];
    fireEvent.click(last);
  }
}];
toggle.story = {
  name: 'Toggle',
  parameters: {
    performance: {
      interactions: interactionTasks
    }
  }
};