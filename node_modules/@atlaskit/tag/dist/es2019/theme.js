import * as colors from '@atlaskit/theme/colors';
import { defaultBorderRadius, defaultRoundedBorderRadius } from './constants';
export const textColors = {
  standard: {
    light: `var(--ds-text, ${colors.N700})`,
    dark: `var(--ds-text, ${colors.DN600})`
  },

  /* eslint-disable @atlaskit/design-system/ensure-design-token-usage */
  green: {
    light: `var(--ds-text, ${colors.N800})`,
    dark: `var(--ds-text, ${colors.N800})`
  },
  purple: {
    light: `var(--ds-text, ${colors.N800})`,
    dark: `var(--ds-text, ${colors.N800})`
  },
  red: {
    light: `var(--ds-text, ${colors.N800})`,
    dark: `var(--ds-text, ${colors.N800})`
  },
  yellow: {
    light: `var(--ds-text, ${colors.N800})`,
    dark: `var(--ds-text, ${colors.N800})`
  },
  grey: {
    light: colors.N0,
    dark: colors.N0
  },
  teal: {
    light: `var(--ds-text, ${colors.N800})`,
    dark: `var(--ds-text, ${colors.N800})`
  },
  blue: {
    light: `var(--ds-text, ${colors.N800})`,
    dark: `var(--ds-text, ${colors.N800})`
  },
  tealLight: {
    light: colors.N500,
    dark: colors.N500
  },

  /* eslint-enable @atlaskit/design-system/ensure-design-token-usage */
  blueLight: {
    light: `var(--ds-text-brand, ${colors.B500})`,
    dark: `var(--ds-text-brand, ${colors.B500})`
  },
  greenLight: {
    light: `var(--ds-text-success, ${colors.G500})`,
    dark: `var(--ds-text-success, ${colors.G500})`
  },
  purpleLight: {
    light: `var(--ds-text-discovery, ${colors.P500})`,
    dark: `var(--ds-text-discovery, ${colors.P500})`
  },
  redLight: {
    light: `var(--ds-text-danger, ${colors.N500})`,
    dark: `var(--ds-text-danger, ${colors.N500})`
  },
  yellowLight: {
    light: `var(--ds-text-warning, ${colors.N500})`,
    dark: `var(--ds-text-warning, ${colors.N500})`
  },
  // eslint-disable-next-line @atlaskit/design-system/ensure-design-token-usage
  greyLight: {
    light: colors.N500,
    dark: colors.N500
  }
};
export const backgroundColors = {
  standard: {
    light: `var(--ds-background-neutral, ${colors.N20})`,
    dark: `var(--ds-background-neutral, ${colors.DN100A})`
  },

  /* eslint-disable @atlaskit/design-system/ensure-design-token-usage */
  green: {
    light: colors.G200,
    dark: colors.G200
  },
  purple: {
    light: colors.P100,
    dark: colors.P100
  },
  red: {
    light: colors.R100,
    dark: colors.R100
  },
  yellow: {
    light: colors.Y200,
    dark: colors.Y200
  },
  grey: {
    light: colors.N500,
    dark: colors.N500
  },
  teal: {
    light: colors.T200,
    dark: colors.T200
  },
  blue: {
    light: colors.B100,
    dark: colors.B100
  },
  tealLight: {
    light: colors.T100,
    dark: colors.T100
  },

  /* eslint-enable @atlaskit/design-system/ensure-design-token-usage */
  blueLight: {
    light: `var(--ds-background-accent-blue, ${colors.B75})`,
    dark: `var(--ds-background-accent-blue, ${colors.B75})`
  },
  greenLight: {
    light: `var(--ds-background-accent-green, ${colors.G100})`,
    dark: `var(--ds-background-accent-green, ${colors.G100})`
  },
  purpleLight: {
    light: `var(--ds-background-accent-purple, ${colors.P75})`,
    dark: `var(--ds-background-accent-purple, ${colors.P75})`
  },
  redLight: {
    light: `var(--ds-background-accent-red, ${colors.R75})`,
    dark: `var(--ds-background-accent-red, ${colors.R75})`
  },
  yellowLight: {
    light: `var(--ds-background-accent-orange, ${colors.Y100})`,
    dark: `var(--ds-background-accent-orange, ${colors.Y100})`
  },
  // eslint-disable-next-line @atlaskit/design-system/ensure-design-token-usage
  greyLight: {
    light: colors.N30,
    dark: colors.N30
  }
};
export const linkHoverBackgroundColors = {
  standard: {
    light: `var(--ds-background-neutral-hovered, ${colors.N30})`,
    dark: `var(--ds-background-neutral-hovered, ${colors.DN60})`
  },

  /* eslint-disable @atlaskit/design-system/ensure-design-token-usage */
  green: {
    light: colors.G100,
    dark: colors.DN60
  },
  purple: {
    light: colors.P75,
    dark: colors.DN60
  },
  red: {
    light: colors.R75,
    dark: colors.DN60
  },
  yellow: {
    light: colors.Y100,
    dark: colors.DN60
  },
  grey: {
    light: colors.N50,
    dark: colors.DN60
  },
  teal: {
    light: colors.T100,
    dark: colors.DN60
  },
  blue: {
    light: colors.B75,
    dark: colors.DN60
  },
  tealLight: {
    light: colors.T75,
    dark: colors.DN60
  },
  blueLight: {
    light: colors.B50,
    dark: colors.DN60
  },
  greenLight: {
    light: colors.G75,
    dark: colors.DN60
  },
  purpleLight: {
    light: colors.P50,
    dark: colors.DN60
  },
  redLight: {
    light: colors.R50,
    dark: colors.DN60
  },
  yellowLight: {
    light: colors.Y75,
    dark: colors.DN60
  },
  greyLight: {
    light: colors.N30,
    dark: colors.DN60
  }
  /* eslint-enable @atlaskit/design-system/ensure-design-token-usage */

};
export const focusRingColors = {
  light: `var(--ds-border-focused, ${colors.B100})`,
  dark: `var(--ds-border-focused, ${colors.B75})`
};
export const linkHoverTextColors = {
  light: `var(--ds-link-pressed, ${colors.B300})`,
  dark: `var(--ds-link-pressed, ${colors.B200})`
};
export const linkActiveBackgroundColors = {
  light: `var(--ds-background-neutral-pressed, ${colors.N30})`,
  dark: `var(--ds-background-neutral-pressed, ${colors.DN600})`
};
export const borderRadius = {
  default: defaultBorderRadius,
  rounded: defaultRoundedBorderRadius
};
export const removalHoverBackgroundColors = {
  light: `var(--ds-background-danger-hovered, ${colors.R50})`,
  dark: `var(--ds-background-danger-hovered, ${colors.R100})`
};
export const removalActiveBackgroundColors = {
  light: `var(--ds-background-danger-pressed, ${colors.R50})`,
  dark: `var(--ds-background-danger-pressed, ${colors.DN600})`
};
export const removalTextColors = {
  light: `var(--ds-text, ${colors.R500})`,
  dark: `var(--ds-text, ${colors.DN30})`
};