import _extends from "@babel/runtime/helpers/extends";
import React from 'react';
import Button from '@atlaskit/button/custom-theme-button';
import { ActionItem, Actions, Footer } from '../styled/Content';

const JustifyShim = props => /*#__PURE__*/React.createElement("span", props);

export default class ModalFooter extends React.Component {
  render() {
    const {
      actions,
      appearance,
      component,
      onClose,
      showKeyline
    } = this.props;
    const warning = 'You can provide `component` OR `actions`, not both.';

    if (!component && !actions) {
      return null;
    }

    if (component && actions) {
      console.warn(warning); // eslint-disable-line no-console

      return null;
    }

    if (component) {
      return /*#__PURE__*/React.createElement(component, {
        appearance,
        onClose,
        showKeyline
      });
    }

    return /*#__PURE__*/React.createElement(Footer, {
      showKeyline: showKeyline
    }, /*#__PURE__*/React.createElement(JustifyShim, null), /*#__PURE__*/React.createElement(Actions, null, actions ? actions.map(({
      text,
      ...rest
    }, index) => {
      const variant = index !== 0 ? 'subtle' : appearance || 'primary';
      return /*#__PURE__*/React.createElement(ActionItem, {
        key: index
      }, /*#__PURE__*/React.createElement(Button, _extends({
        appearance: variant
      }, rest), text));
    }) : null));
  }

}