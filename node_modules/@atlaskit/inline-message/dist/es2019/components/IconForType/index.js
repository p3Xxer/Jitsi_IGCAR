import React from 'react';
import { typesMapping } from '../../constants';
import IconWrapper from './styledIconForType';
export default class SelectedIconForType extends React.Component {
  render() {
    const {
      type,
      isHovered,
      isOpen
    } = this.props;
    const {
      [type]: {
        icon: SelectedIcon,
        iconSize
      }
    } = typesMapping;
    return /*#__PURE__*/React.createElement(IconWrapper, {
      appearance: type,
      isHovered: isHovered,
      isOpen: isOpen
    }, /*#__PURE__*/React.createElement(SelectedIcon, {
      label: "Inline message icon",
      size: iconSize
    }));
  }

}