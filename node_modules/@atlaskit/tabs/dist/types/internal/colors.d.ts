import { ThemeModes } from '@atlaskit/theme/types';
export declare type NavItemColors = {
    labelColor: string;
    activeLabelColor: string;
    hoverLabelColor: string;
    selectedColor: string;
};
export declare const getNavItemColors: (mode: ThemeModes) => NavItemColors;
export declare type NavLineColors = {
    lineColor: string;
    selectedColor: string;
};
export declare const getNavLineColor: (mode: ThemeModes) => NavLineColors;
