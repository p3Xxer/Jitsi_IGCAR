"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.discardDuplicates = void 0;
var postcss_1 = require("postcss");
/**
 * Discards top level duplicate declarations.
 */
exports.discardDuplicates = postcss_1.plugin('discard-duplicates', function () {
    var decls = {};
    return function (root) {
        root.each(function (node) {
            if (node.type === 'decl') {
                decls[node.prop] = decls[node.prop] || [];
                decls[node.prop].push(node);
            }
        });
        for (var key in decls) {
            var found = decls[key];
            for (var i = 0; i < found.length - 1; i++) {
                found[i].remove();
            }
        }
    };
});
//# sourceMappingURL=discard-duplicates.js.map