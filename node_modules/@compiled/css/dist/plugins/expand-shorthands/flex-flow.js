"use strict";
var __spreadArrays = (this && this.__spreadArrays) || function () {
    for (var s = 0, i = 0, il = arguments.length; i < il; i++) s += arguments[i].length;
    for (var r = Array(s), k = 0, i = 0; i < il; i++)
        for (var a = arguments[i], j = 0, jl = a.length; j < jl; j++, k++)
            r[k] = a[j];
    return r;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.flexFlow = void 0;
var utils_1 = require("./utils");
/**
 * https://developer.mozilla.org/en-US/docs/Web/CSS/flex-flow
 */
var flexFlow = function (value) {
    var _a = value.nodes, left = _a[0], right = _a[1];
    var directionValues = __spreadArrays(utils_1.globalValues, ['row', 'row-reverse', 'column', 'column-reverse']);
    var wrapValues = __spreadArrays(utils_1.globalValues, ['nowrap', 'wrap', 'reverse']);
    var directionValue = '';
    var wrapValue = '';
    /**
     * Extracts values from a node and mutates variables in scope.
     * If it returns `true` we should bail out and return no nodes.
     *
     * @param node
     */
    var extractValues = function (node) {
        if (node && node.type === 'word') {
            if (directionValues.includes(node.value)) {
                if (directionValue !== '') {
                    // Invalid - already set!
                    return true;
                }
                directionValue = node.value;
            }
            else if (wrapValues.includes(node.value)) {
                if (wrapValue !== '') {
                    // Invalid - already set!
                    return true;
                }
                wrapValue = node.value;
            }
            else {
                // Invalid
                return true;
            }
        }
        return false;
    };
    if (extractValues(left) || extractValues(right)) {
        return [];
    }
    return [
        { prop: 'flex-direction', value: directionValue || 'row' },
        { prop: 'flex-wrap', value: wrapValue || 'nowrap' },
    ];
};
exports.flexFlow = flexFlow;
//# sourceMappingURL=flex-flow.js.map