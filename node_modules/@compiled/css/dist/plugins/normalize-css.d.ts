import { Plugin } from 'postcss';
/**
 * This plugin runs a subset of the cssnao plugins to normalize CSS during build.
 * During a production build it will run more plugins.
 */
export declare const normalizeCSS: () => Plugin<never>[];
