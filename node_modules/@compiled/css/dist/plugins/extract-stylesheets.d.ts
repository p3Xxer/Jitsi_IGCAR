/**
 * PostCSS plugin which will callback when traversing through each root declaration.
 */
export declare const extractStyleSheets: import("postcss").Plugin<{
    callback: (sheet: string) => void;
}>;
